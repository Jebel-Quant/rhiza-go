<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# rhiza\-go

```go
import "github.com/jebel-quant/rhiza-go/cmd/rhiza-go"
```

Command rhiza\-go is a starter application demonstrating the rhiza\-go template structure.

This is example code — replace it with your own application logic. It shows how to use the standard Go project layout:

- cmd/ for application entry points
- pkg/ for public library packages
- internal/ for private internal packages

## Index



# utils

```go
import "github.com/jebel-quant/rhiza-go/internal/utils"
```

Package utils provides utility functions for the rhiza\-go application.

This is starter code — replace or extend it with your own utility functions.

## Index

- [func Contains\(slice \[\]string, item string\) bool](<#Contains>)
- [func SanitizePath\(path string\) string](<#SanitizePath>)


<a name="Contains"></a>
## func [Contains](<https://github.com/Jebel-Quant/rhiza-go/blob/main/internal/utils/utils.go#L24>)

```go
func Contains(slice []string, item string) bool
```

Contains checks if a string slice contains a specific string

<a name="SanitizePath"></a>
## func [SanitizePath](<https://github.com/Jebel-Quant/rhiza-go/blob/main/internal/utils/utils.go#L18>)

```go
func SanitizePath(path string) string
```

SanitizePath normalizes a file path using filepath.Clean.

Note: This does NOT prevent directory traversal attacks. The function preserves relative path components \(like ../../../etc/passwd\). Callers must validate the result is within allowed directories if security is a concern.

For secure path handling, use filepath.Join with a base directory and verify the result with filepath.Rel or strings.HasPrefix to ensure it stays within bounds.

# config

```go
import "github.com/jebel-quant/rhiza-go/pkg/config"
```

Package config handles loading and managing configuration for the rhiza\-go application.

This is starter code — replace or extend it with your own configuration logic.

## Index

- [type Config](<#Config>)
  - [func Load\(\) \(\*Config, error\)](<#Load>)
- [type Template](<#Template>)
  - [func LoadTemplate\(path string\) \(\*Template, error\)](<#LoadTemplate>)


<a name="Config"></a>
## type [Config](<https://github.com/Jebel-Quant/rhiza-go/blob/main/pkg/config/config.go#L16-L19>)

Config represents the rhiza\-go configuration

```go
type Config struct {
    Version   string `yaml:"version"`
    GoVersion string `yaml:"go_version"`
}
```

<a name="Load"></a>
### func [Load](<https://github.com/Jebel-Quant/rhiza-go/blob/main/pkg/config/config.go#L23>)

```go
func Load() (*Config, error)
```

Load returns a Config with default values and, if present, overrides GoVersion by reading it from a .go\-version file in the current directory.

<a name="Template"></a>
## type [Template](<https://github.com/Jebel-Quant/rhiza-go/blob/main/pkg/config/config.go#L40-L45>)

Template represents a rhiza template configuration

```go
type Template struct {
    Repository string   `yaml:"repository"`
    Ref        string   `yaml:"ref"`
    Include    []string `yaml:"include"`
    Exclude    []string `yaml:"exclude"`
}
```

<a name="LoadTemplate"></a>
### func [LoadTemplate](<https://github.com/Jebel-Quant/rhiza-go/blob/main/pkg/config/config.go#L48>)

```go
func LoadTemplate(path string) (*Template, error)
```

LoadTemplate reads template configuration from .rhiza/template.yml

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
